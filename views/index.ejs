<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- CSS only -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi"
      crossorigin="anonymous"
    />
    <title>FileUploads</title>
    <style>
      img {
        width: 100%;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="row">
        <div class="col-md-6 m-auto">
          <h1 class="text-center my-4 text-secondary">Hellos there</h1>
          <form action="/upload" method="post" enctype="multipart/form-data">
            <div class="custom-file mb-3">
              <input type="file" name="file" id="file" class="form-control" />
            </div>
            <div class="d-grid">
              <input type="submit" value="Submit" class="btn btn-primary" />
            </div>
          </form>
          <hr />
          <% if(files){ %> <% files.forEach((file) => { %>
          <div class="card card-body mb-3">
            <% if(file.isImage){ %>
            <img src="image/<%= file.filename %>" alt="Image" />
            <% }else{ %> <%= file.filename %> <% } %>
            <div
              class="d-flex flex-row justify-content-center align-items-center mt-2"
            >
              <form
                action="/files/<%= file._id %>?_method=DELETE"
                method="post"
                class="me-3"
              >
                <button class="btn btn-danger" type="submit">Delete</button>
              </form>
              <form action="/download/<%= file._id %>" method="get">
                <button class="btn btn-success" type="submit">Download</button>
              </form>
            </div>
            <!-- <form action="/files/<%= file._id %>?_method=DELETE" method="post">
              <div class="row">
                <button class="btn btn-danger" type="submit">Delete</button>
              </div>
            </form>
            <form action="/download/<%= file._id %>" method="get" class="mt-2">
              <div class="row">
                <button class="btn btn-success" type="submit">Download</button>
              </div>
            </form> -->
          </div>
          <% }) %> <% }else{ %>
          <p>No files to show</p>
          <% } %>
        </div>
      </div>
    </div>
    <!-- JavaScript Bundle with Popper -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
